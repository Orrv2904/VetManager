<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="VetManager.home">
    <ContentPage.Content>
        <Grid>
            <!-- Contenido principal de la página -->

            <!-- Botón para mostrar/ocultar el menú -->
            <Button Text="☰"
                    FontSize="24"
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"
                    Margin="10"
                    Clicked="ToggleMenuButton_Clicked"
                    Grid.Row="0" />

            <!-- Menú desplegable -->
            <pancake:PancakeView x:Name="menuFrame"
                                 BackgroundColor="#2196F3"
                                 CornerRadius="10"
                                 Padding="10"
                                 IsVisible="False"
                                 AbsoluteLayout.LayoutFlags="WidthProportional, HeightProportional"
                                 AbsoluteLayout.LayoutBounds="1,0,0.6,1"
                                 Grid.Row="0">
                <!-- Contenido del menú -->
                <StackLayout Spacing="10">
                    <Label Text="Opción 1" TextColor="White" FontSize="18" HorizontalOptions="Center" />
                    <Label Text="Opción 2" TextColor="White" FontSize="18" HorizontalOptions="Center" />
                    <Label Text="Opción 3" TextColor="White" FontSize="18" HorizontalOptions="Center" />
                </StackLayout>
            </pancake:PancakeView>
        </Grid>
    </ContentPage.Content>
</ContentPage>